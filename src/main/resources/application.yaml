#spring.application.name=GraphanaApp


spring:
  file:
    storage:
      location: C:/Users/falodun.ebenezer/Desktop/Ongoing projects 2024
  application:
    name: GraphanaApp
  datasource:
    url: jdbc:mysql://localhost:3306/graphanadb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect